DBMS Project

Chris:
   Set up tokenizer
   Handle semicolon-terminated multi-line statements
   Implement recursive descent parsing for every commands
   Return parsed command objects
   Implement CREATE TABLE and DESCRIBE
   Implement DELETE execution
 
Sasha:
   Implement RENAME
   Define and save table headers
   Implement CREATE DATABASE and USE
   Implement UPDATE execution
   Make sure EXIT and restart restore state

Randy:
   Design record format and record pointer system
   Implement BST index build/save/load
   Implement key insert/search/delete in BST
   Support in-order traversal behavior
   Implement SELECT output formatting
   Implement WHERE condition evaluation

Jalen:
   Design database/table metadata structures
   Implement INSERT validation and execution
   Implement LET and INPUT/OUTPUT
   Normalize case-insensitive keywords
